# Recipe 1. PHP with curl must be on PATH. Replace the absolute bridge path.
# Set PL_MCP_URL and PL_MCP_TOKEN privately before starting the client.
[mcp_servers.phpledger]
command = "php"
args = ["/absolute/path/to/phpledger/tools/mcp-bridge.php"]
env_vars = ["PL_MCP_URL", "PL_MCP_TOKEN"]
