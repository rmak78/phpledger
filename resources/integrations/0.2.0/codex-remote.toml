# Recipe 1. Replace the example URL. Supply the token in a private process environment.
[mcp_servers.phpledger]
url = "https://ledger.example.com/mcp"
bearer_token_env_var = "PL_MCP_TOKEN"
# For per-user OAuth, omit bearer_token_env_var and use codex mcp login phpledger.
