# Minimal OpenSSL settings for creating PHP Ledger's private keys.
# Used only when the host's own OpenSSL configuration file is missing,
# as on some Windows stacks such as XAMPP. It contains no secrets.
[ req ]
distinguished_name = req_distinguished_name

[ req_distinguished_name ]
