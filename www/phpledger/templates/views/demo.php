<?php declare(strict_types=1); ?>
<section class="auth-panel grid grid-cols-1 gap-8 lg:grid-cols-[1fr_260px]">
    <div><p class="eyebrow">The latest working preview</p><?php pl_ui_page_header('A small business. A clear set of books. Your turn.', 'Step inside PHP Ledger with your own temporary sample company. Choose the accounting starter playground for invoices, bills, purchasing, stock and partial payments. Or explore one of eleven businesses with 2024–2025 history and an open 2026 practice year.'); ?>
    <details class="mb-3"><summary class="text-xs text-ink-muted">Your regional starting point</summary><?php require __DIR__ . '/../partials/regional.php'; ?></details>
    <form method="post" action="<?= pl_e(pl_url('/start')) ?>"><?= pl_csrf_field() ?><label class="field mb-3">Choose your sample business<select class="select" name="sample_pack" required><?php foreach (pl_demo_sample_choices() as $entry): ?><option value="<?= pl_e($entry['id']) ?>"><?= pl_e($entry['id'] === 'accounting-starter' ? $entry['name'] : $entry['business'] . ' - ' . $entry['name']) ?></option><?php endforeach; ?></select><span class="field-hint">The starter playground begins with zero balances and prepared products, a party and sample tax. Each historical choice has 74 sources, 36 month checkpoints and three practice drafts. Only the selected company is provisioned.</span></label><label class="field mb-3">Sample country / currency<select class="select" name="currency"><?php foreach (pl_base_currency_options() as $currency => $label): ?><option value="<?= pl_e($currency) ?>" <?= ($regional['currency'] ?? 'USD') === $currency ? 'selected' : '' ?>><?= pl_e($label) ?></option><?php endforeach; ?></select><span class="field-hint">Choose the currency for your new sample. Amounts are illustrative; country accounting and tax rules are not enabled.</span></label><button type="submit" class="btn btn-primary" data-fold="primary action">Start my sample <?= pl_icon('arrow-right') ?></button></form>
    <p class="muted small">No registration. No real business information needed.</p><p class="muted small">Catalogue status is shown in the pack metadata: the four retained histories are demo-only releases; the seven newer vertical scenarios are preview-only review material and are restricted to isolated sample companies.</p><a class="back-link" href="/">About PHP Ledger</a></div>
    <aside class="panel self-start text-sm"><p class="eyebrow">What to try</p><ol class="journey-steps"><li><strong>Receive goods</strong><span>Order sample goods, receive them and match a supplier bill.</span></li><li><strong>Invoice and collect</strong><span>Post an invoice and record a partial payment against it.</span></li><li><strong>Reconcile your balances</strong><span>Compare outstanding invoices, bills and stock with the ledger.</span></li></ol><div class="demo-boundary"><strong>Made for exploring.</strong><p>Each visitor receives separate sample books. All demo records reset at the next hour. Deletion, imports, user administration and period changes are disabled. Posted entries retain their history; corrections use linked reversals.</p><p>This is a development preview. Do not enter personal, customer or real financial information.</p></div></aside>
</section>
